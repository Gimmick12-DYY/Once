{
  "name": "once",
  "version": "1.0.0",
  "description": "Once — Perplexity + Claude → Markdown notes (vault folder or Obsidian CLI)",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "once": "bin/once.mjs"
  },
  "scripts": {
    "once": "node scripts/once.mjs"
  }
}
